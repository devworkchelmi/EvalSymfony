{% extends 'base.html.twig' %}

{% block title %}Liste des boards{% endblock %}

{% block body %}
  <div class="container mt-4 text-light">
    <h1 class="mb-4">📚 Forums disponibles</h1>

    {% if boards is not empty %}
      <div class="row">
        {% for board in boards %}
          <div class="col-md-6 mb-4">
            <div class="card h-100 shadow-sm animate__animated animate__fadeInUp">
              <div class="card-body d-flex flex-column justify-content-between">
                <div>
                  <h5 class="card-title">🧩 {{ board.name }}</h5>
                  <p class="card-text text-muted">{{ board.description }}</p>
                  <p class="small text-muted">Catégorie : {{ board.category.name }}</p>
                </div>
                <div class="d-flex justify-content-between mt-3 ">
                  <a href="{{ path('forum_board', { id: board.id }) }}" class="btn btn-outline-dark btn-sm">
                    🔎 Voir les sujets
                  </a>
                  {% if app.user %}
                    <a href="{{ path('topic_new', { id: board.id }) }}" class="btn btn-success btn-sm">
                      ➕ Nouveau sujet
                    </a>
                  {% endif %}
                </div>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    {% else %}
      <div class="alert alert-info">Aucun board n'est disponible actuellement.</div>
    {% endif %}
  </div>

  {# CDN animate.css #}
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
{% endblock %}
