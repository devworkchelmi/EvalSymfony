{# templates/forum/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Accueil du Forum{% endblock %}

{% block body %}
  <h1 class="mb-4">📚 Catégories</h1>

  <div class="row">
    {% set categories = [
      { name: 'Insider', description: 'Accès réservé aux employés internes de l\'entreprise.' },
      { name: 'Collaborateur', description: 'Pour les partenaires et intervenants externes.' },
      { name: 'Externe', description: 'Accès limité aux utilisateurs publics ou invités.' }
    ] %}

    {% for category in categories %}
      <div class="col-md-4 mb-4">
        <div class="card shadow-sm h-100">
          <div class="card-header bg-dark text-white fw-bold">
            {{ category.name }}
          </div>
          <div class="card-body">
            <p class="card-text">{{ category.description }}</p>
            <a href="#" class="btn btn-outline-primary w-100">Voir les boards</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
{% endblock %}
