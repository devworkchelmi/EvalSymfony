{% extends 'base.html.twig' %}

{% block title %}Mes documents
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1 class="mb-4">📄 Mes documents</h1>

		<div class="mb-3">
			<a href="{{ path('app_document_new') }}" class="btn btn-success">➕ Nouveau document</a>
		</div>

		{% if documents is not empty %}
			<ul class="list-group">
				{% for document in documents %}
					<li class="list-group-item d-flex justify-content-between align-items-center">
						<div>
							📄
							{{ document.fileName }}<br>
							<small class="text-muted">Ajouté le
								{{ document.createdAt|date('d/m/Y H:i') }}</small>
						</div>

						<a href="{{ path('app_document_show', { id: document.id }) }}" class="btn btn-sm btn-primary">
							Voir le document
						</a>
					</li>
				</a>
			</li>
		{% endfor %}
	</ul>
{% else %}
	<div class="alert alert-info mt-4">Aucun document disponible.</div>
{% endif %}</div>{% endblock %}
