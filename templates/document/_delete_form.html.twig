<form method="post" action="{{ path('app_document_delete', {'fileName': document.fileName}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.fileName) }}">
    <button class="btn">Delete</button>
</form>
